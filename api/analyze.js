export default async function handler(req, res) {
    if (req.method !== 'POST') {
        return res.status(405).json({ error: 'Method Not Allowed' });
    }

    const { text: userInput, situation, forbiddenWords } = req.body;
    
    let apiKey = process.env.GOOGLE_API_KEY || "";
    apiKey = apiKey.replace(/['"]/g, '').trim(); 

    if (!apiKey) return res.status(500).json({ error: "ไม่พบ API Key ในระบบหลังบ้าน" });
    if (!userInput) return res.status(400).json({ error: "กรุณากรอกข้อความ" });

    const FORBIDDEN = Array.isArray(forbiddenWords) && forbiddenWords.length > 0 
        ? forbiddenWords 
        : ['ขี้เกียจ', 'ภาระ'];

    for (let word of FORBIDDEN) {
        if (userInput.includes(word)) {
            return res.status(200).json({ 
                text: `### 🚨 ตรวจพบคำต้องห้ามร้ายแรง!\n\nระบบพบคำว่า: **"${word}"**\n\n⚠️ **การสื่อสารล้มเหลวทันที** เนื่องจากมีการใช้คำพูดที่อาจกระตุ้นให้สถานการณ์เลวร้ายลง หรือแสดงเจตนาที่ไม่เหมาะสมในภาวะวิกฤต\n\n[จบการวิเคราะห์]\n\nSCORE: 0\nTONE: Aggressive` 
            });
        }
    }

    const currentSituation = situation || "สถานการณ์ทั่วไป";

    try {
        const prompt = `คุณคือ AI ผู้เชี่ยวชาญด้านการสื่อสารในภาวะวิกฤต (Crisis Communication Specialist)
สถานการณ์ที่เกิดขึ้น: "${currentSituation}"
คำพูดของผู้ใช้งาน: "${userInput}"

หน้าที่ของคุณคือการวิเคราะห์และสร้างรายงานที่สวยงาม เข้าใจง่าย โดยปฏิบัติตามนี้:
1. **ประเมินคะแนนความเป็นมืออาชีพ** (0-100) โดยพิจารณาจากความเห็นอกเห็นใจ, การให้ข้อมูลที่ชัดเจน, การควบคุมอารมณ์ และการเสนอทางออก
2. **วิเคราะห์จุดเด่นและจุดที่ควรปรับปรุง** โดยใช้ Bullet point และมี Emoji ประกอบหัวข้อ
3. **สร้างตารางเปรียบเทียบ (Markdown Table)** ระหว่าง "คำพูดเดิม" กับ "คำพูดที่แนะนำ (Best Practice)" โดยในช่องคำพูดที่แนะนำ ให้ใช้ **ตัวหนา (Bold)** เน้นตรงคีย์เวิร์ดสำคัญที่แสดงถึงการเปลี่ยนแปลงที่ดีขึ้น
4. **ระบุอารมณ์เด่น (Tone)** มา 1 อย่างจาก [Aggressive, Professional, Passive, Neutral]
5. **ใช้ Emoji** ตกแต่งหัวข้อและประโยคสำคัญๆ อย่างสม่ำเสมอเพื่อให้รายงานดูน่าอ่าน ไม่น่าเบื่อ

⚠️ **รูปแบบการตอบกลับ (Strict Output Format):**
- เริ่มต้นด้วยหัวข้อใหญ่: ### 📊 รายงานผลการวิเคราะห์เชิงลึก
- บรรทัดต่อมา บังคับระบุคะแนน: SCORE: [ตัวเลขคะแนน] (เช่น SCORE: 85)
- เนื้อหาการวิเคราะห์ (ใช้ Emoji เยอะๆ 🌟)
- ตารางเปรียบเทียบ
- บรรทัดสุดท้าย บังคับระบุอารมณ์: TONE: [Aggressive หรือ Professional หรือ Passive หรือ Neutral]
- จบด้วยข้อความ: [จบการวิเคราะห์]`;

        // ใช้ Model ที่เสถียร
        const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}`;

        const response = await fetch(url, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                contents: [{ parts: [{ text: prompt }] }],
                // เพิ่ม safety settings เพื่อลดโอกาสที่ AI จะบล็อกเนื้อหาบางประเภท
                safetySettings: [
                    { category: "HARM_CATEGORY_HARASSMENT", threshold: "BLOCK_NONE" },
                    { category: "HARM_CATEGORY_HATE_SPEECH", threshold: "BLOCK_NONE" },
                    { category: "HARM_CATEGORY_SEXUALLY_EXPLICIT", threshold: "BLOCK_NONE" },
                    { category: "HARM_CATEGORY_DANGEROUS_CONTENT", threshold: "BLOCK_NONE" }
                ]
            })
        });

        const data = await response.json();

        if (!response.ok) {
            throw new Error(data.error?.message || "Google API Error");
        } else if (!data.candidates || data.candidates.length === 0) {
             throw new Error("AI ไม่สามารถสร้างคำตอบได้ในขณะนี้ (No candidates)");
        }

        const report = data.candidates[0].content.parts[0].text;
        return res.status(200).json({ text: report });

    } catch (error) {
        return res.status(500).json({ error: `เกิดข้อผิดพลาด: ${error.message}` });
    }
}