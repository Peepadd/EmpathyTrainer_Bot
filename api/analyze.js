export default async function handler(req, res) {
    if (req.method !== 'POST') {
        return res.status(405).json({ error: 'Method Not Allowed' });
    }

    // รับค่าที่ส่งมาจาก Frontend เพิ่มเติม
    const { text: userInput, situation, forbiddenWords } = req.body;
    
    let apiKey = process.env.GOOGLE_API_KEY || "";
    apiKey = apiKey.replace(/['"]/g, '').trim(); 

    if (!apiKey) return res.status(500).json({ error: "ไม่พบ API Key ในระบบหลังบ้าน" });
    if (!userInput) return res.status(400).json({ error: "กรุณากรอกข้อความ" });

    // ใช้คำต้องห้ามที่ส่งมา ถ้าไม่มีให้ใช้ค่าเริ่มต้น
    const FORBIDDEN = Array.isArray(forbiddenWords) && forbiddenWords.length > 0 
        ? forbiddenWords 
        : ['ขี้เกียจ', 'ภาระ'];

    for (let word of FORBIDDEN) {
        if (userInput.includes(word)) {
            // คืนค่ารูปแบบที่มี SCORE และ TONE ซ่อนอยู่ เพื่อให้หน้าเว็บไปอัปเดตวงกลมคะแนนให้เป็น 0%
            return res.status(200).json({ 
                text: `### 🚨 ผิดกฎร้ายแรง\n\nพบคำต้องห้าม: **[${word}]**\n\nการสื่อสารล้มเหลวทันที เนื่องจากใช้คำที่กระตุ้นให้สถานการณ์แย่ลง\n\n[จบการวิเคราะห์]\n\nSCORE: 0\nTONE: Aggressive` 
            });
        }
    }

    // ใช้สถานการณ์ที่ส่งมา ถ้าไม่มีให้ใช้ค่าเริ่มต้น
    const currentSituation = situation || "เพื่อนในกลุ่มหายตัวไป ไม่อ่านไลน์ พรุ่งนี้มีพรีเซนต์";

    try {
        const prompt = `คุณคือระบบ AI วิเคราะห์ทักษะการสื่อสารในภาวะวิกฤต 
สถานการณ์: [${currentSituation}]
คำพูดผู้เล่น: "${userInput}"

งานของคุณ:
1. ประเมินความเป็นมืออาชีพ ให้คะแนน 0-100 (ระบุแค่ตัวเลขเท่านั้นในส่วนคะแนน)
2. วิเคราะห์ข้อดีและข้อเสีย
3. สร้างตารางเปรียบเทียบระหว่าง "คำพูดเดิมของผู้เล่น" กับ "คำพูดที่แนะนำ (Best Practice)"
4. ไม่ทำให้ข้อความเยอะเกินไป
5. ระบุอารมณ์เด่นของประโยคเพียง 1 อย่าง (ระบุ TONE: [Aggressive/Professional/Passive/Neutral])

⚠️ กฎการตอบแบบ Markdown:
- ขึ้นต้นด้วยหัวข้อ ### 📊 ผลการวิเคราะห์เชิงลึก
- บรรทัดถัดมาต้องเขียนว่า SCORE: [ตัวเลขคะแนน] (เช่น SCORE: 85) เพื่อให้ระบบดึงไปทำ Dashboard
- ใช้ตาราง Markdown เปรียบเทียบคำพูด
- ใช้ Emoji ตกแต่งให้สวยงามน่าอ่าน
- ปิดท้ายด้วย [จบการวิเคราะห์]`;

        const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}`;

        const response = await fetch(url, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                contents: [{ parts: [{ text: prompt }] }]
            })
        });

        const data = await response.json();

        if (!response.ok) {
            throw new Error(data.error?.message || "Google API Error");
        }

        const report = data.candidates[0].content.parts[0].text;
        return res.status(200).json({ text: report });

    } catch (error) {
        return res.status(500).json({ error: `AI Error: ${error.message}` });
    }
}